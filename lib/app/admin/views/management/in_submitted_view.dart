import 'package:flutter/material.dart';

class InSubmittedView extends StatelessWidget {
  const InSubmittedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Diajukan"));
  }
}
